# Core ML (CPU only — no CUDA required)
torch>=2.1.0
numpy>=1.24.0
scikit-learn>=1.3.0
pandas>=2.0.0
pyyaml>=6.0
tqdm>=4.66.0

# Static analysis
pefile>=2023.2.7,<2025.0.0

# Graph / viz (optional, for thesis figures)
matplotlib>=3.7.0
seaborn>=0.13.0

# HTTP + env (VM only)
requests>=2.31.0
python-dotenv>=1.0.0
pyzipper>=0.3.6
